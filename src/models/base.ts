import Model from '@/utils/model'

interface IState {
    Colors: Record<string, any>
}
export default {
    namespace: 'Base',
    state: {
    } as IState,
    effects: {
    },
    reducers: {
    }
} as Model